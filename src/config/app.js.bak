/**
 * App Configuration
 * Toggle between mock data (demo mode) and real API (production mode)
 */

export const AppConfig = {
  // 🎭 DEMO MODE TOGGLE
  // Set to true for UI demos with mock data
  // Set to false for real backend integration
  USE_MOCK_DATA: false,
  
  // API Configuration
  API_URL: __DEV__ 
    ? 'http://192.168.1.19:5001/api'
    : 'https://your-production-api.com/api',
  
  // Feature Flags
  FEATURES: {
    REAL_TIME_CHAT: false,
    PUSH_NOTIFICATIONS: false,
    STRIPE_PAYMENTS: true,
    IMAGE_UPLOADS: false,
  },
  
  // App Info
  VERSION: '1.0.0',
  IS_DEV: __DEV__,
};

// Helper functions
export const useMockData = () => AppConfig.USE_MOCK_DATA;
export const getApiUrl = () => AppConfig.API_URL;
